---
name: btw
description: A pocket aside in the same chat — not a job, not project-scope. Use when the human says btw, side quest, aside, or is just talking / curious / pointing at a direction with no task. Does not go to the sky, does not promote, does not touch registers. Keywords - btw, side quest, sidequest, aside, just curious, by the way, not a task, just talking.
---

# btw — a pocket, not a job

The light counterpart to `discuss`. `discuss` is project-shaped thinking: output to the sky, then
"promote anything?" **`btw` (alias `side quest` / `aside`) is not that.** It is an utterance that is
not a job. Same chat — the manager stays the mouth — no second session, no sky, no promote.

## What this is

- **Not a job.** No done-condition, no STATE update, no backlog item, no "fixed."
- **Not `discuss`.** Nothing here is a candidate for promotion. Do not offer. Do not file it in the sky
  "just in case."
- **Weight, not a thin close.** If they pointed with examples, work the *class* (A2), not the named
  member. One-reply finish is fine when the right thing is in view; a finish that only treats the
  invoice / the word "defects" / the nearest handle is underweighted.
- **Do not ask them to disambiguate.** Extract the direction. Questions are blockers; they already
  dumped context so you could decide.
- Long is allowed. The length guard stands down for this pocket. Do not dump the reply into STATE
  to "comply" with D14 — there was no work.

## The exit

They leave by going back to the work — a plain instruction, `go ahead` on a task, another code word.
Resume where the project stood. The aside changed nothing. Do not recap it into the registers.
